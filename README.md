OSC Listener for Processing
===========================

This represents a simple framework for creating visual artifacts that listen to OSC data.
It includes a working example that uses the framework.
